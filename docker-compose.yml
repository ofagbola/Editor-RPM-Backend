version: '3.8'

services:
  postgres:
    build: 
      context: ./
      dockerfile: Dockerfile
    # image: postgres
    # container_name: postgres-db
    env_file:
      - .env
    ports:
      - "5432:5432"
    volumes:
      - pgdata2:/var/lib/postgresql/data
volumes:
  pgdata2:
